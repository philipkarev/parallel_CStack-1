#ifndef CSTACK_FUNCTIONS_H
#define CSTACK_FUNCTIONS_H

#include <iostream>
#include <cstring>

char* mygets(char*&, size_t&, FILE*);
int get_int(char*, int&);
int get_word(char*, char*&);

#endif //CSTACK_FUNCTIONS_H
